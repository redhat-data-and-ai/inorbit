# Local demo poll intervals (live uses the same defaults).
# SLA clock has no I/O. Airflow/quality pollers only HTTP/warehouse in INORBIT_MODE=live.
mode: demo
http_addr: ":8080"
clock_interval_seconds: 15
astro_run_poll_seconds: 90
astro_tag_poll_seconds: 600
quality_poll_seconds: 120
